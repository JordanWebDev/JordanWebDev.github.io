/**
 * Works Components Barrel Export
 */
export { MasonryGrid } from './MasonryGrid';
export { ProjectCard } from './ProjectCard';
export { ProjectModal } from './ProjectModal';

// Re-export types for convenience
export type {
    ProjectData,
    ProjectHeight,
    ProjectCategory,
    MasonryGridProps,
    ProjectCardProps,
    ProjectModalProps,
} from '../../types/works.types';
