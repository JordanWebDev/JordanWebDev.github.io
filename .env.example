# Environment Variables
# Copy this file to .env and fill in your values

# ===========================================
# GITHUB API (for Works page)
# ===========================================

# Your GitHub username (required)
VITE_GITHUB_USERNAME=JordanWebDev

# GitHub Personal Access Token (optional, enables pinned repos via GraphQL)
# Create at: https://github.com/settings/tokens
# Required scopes: public_repo (read-only access to public repos)
# Without this token, falls back to REST API with top starred repos
VITE_GITHUB_TOKEN=
